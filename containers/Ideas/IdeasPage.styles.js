import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
    padding: 32,
    paddingBottom: 70
  },
  absoluteBottomBtn: {
    // alignSelf: 'flex-end',
    // position: 'absolute',
    // bottom: 35
  }
});
