import { StyleSheet } from 'react-native';

// *** SAMPLE STYLES ***

export const styles = StyleSheet.create({
  inputGroup: {
    flexDirection: 'row'
  },
  textInput: {
    width: 200,
    borderColor: 'black',
    borderWidth: 1
  }
});
